---
title: News Feed System Design
description: Design a social media news feed like Facebook or Twitter.
---

## Problem Statement

Design a news feed system that displays personalized content from friends/followers in real-time.

---

## Requirements

### Functional Requirements
- User can create posts
- User sees feed of posts from people they follow
- Feed sorted by relevance/time
- Support likes, comments, shares
- Real-time updates

### Non-Functional Requirements
- Low latency: under 200ms feed load
- High availability
- Handle 500M+ daily active users
- Scalable to billions of posts

---

## Capacity Estimation

```
Assumptions:
- 500M DAU
- Average user follows 200 people
- Average user creates 2 posts/day
- Average user checks feed 10 times/day

Traffic:
- Writes: 500M × 2 = 1B posts/day ≈ 12K/sec
- Reads: 500M × 10 = 5B feeds/day ≈ 58K/sec
- Read:Write ratio ≈ 5:1

Storage:
- Post: 1 KB average (text, metadata)
- Daily posts: 1B × 1 KB = 1 TB/day
- 5 years: 1.8 PB (without media)
```

---

## Feed Generation Approaches

### Pull Model (Fan-out on Read)

```mermaid
flowchart TB
    U["User A requests feed"] --> FS["Feed Service"]
    FS --> S1["1. Get A's following list: #91;B, C, D, E, F...#93;"]
    S1 --> S2["2. For each followed user, query recent posts from Posts DB"]
    S2 --> S3["3. Merge and sort all posts"]
    S3 --> S4["4. Apply ranking algorithm"]
    S4 --> S5["5. Return top N posts"]
    style FS fill:#dbeafe
    style S5 fill:#dcfce7
```

Pros: Simple, storage efficient
Cons: Slow for users following many people

### Push Model (Fan-out on Write)

```mermaid
flowchart TB
    U["User B creates post"] --> FS["Feed Service"]
    FS --> S1["1. Get B's followers: #91;A, X, Y, Z...#93;"]
    S1 --> S2["2. For each follower, add post to pre-computed feed cache"]
    S2 --> S3["3. Done (async)"]
    S3 --> R["When A requests feed: read from A's pre-computed cache"]
    style FS fill:#dbeafe
    style R fill:#dcfce7
```

Pros: Fast reads
Cons: Celebrity problem (millions of followers)

### Hybrid Approach (Recommended)

```mermaid
flowchart TB
    H["Hybrid Strategy"] --> C["Celebrity (>10K followers): Pull model — fetch posts on demand"]
    H --> R["Regular users (<10K followers): Push model — pre-compute feeds"]
    C --> FG["Feed Generation"]
    R --> FG
    FG --> S1["1. Get pre-computed feed (push)"]
    S1 --> S2["2. Fetch celebrity posts (pull)"]
    S2 --> S3["3. Merge, rank, return"]
    style H fill:#dbeafe
    style S3 fill:#dcfce7
```

---

## High-Level Architecture

```mermaid
flowchart TB
    Clients --> LB["Load Balancer"]
    LB --> PostSvc["Post Service"]
    LB --> FeedSvc["Feed Service"]
    LB --> UserSvc["User Service"]
    PostSvc --> PostsDB["Posts DB (Cassandra)"]
    PostSvc --> Kafka["Kafka (Events)"]
    FeedSvc --> FeedCache["Feed Cache (Redis)"]
    FeedSvc --> Kafka
    UserSvc --> GraphDB["Graph DB (Follows)"]
    Kafka --> Workers["Feed Generator Workers"]
    Workers --> FeedCache
    style LB fill:#dbeafe
    style FeedCache fill:#fef3c7
    style Workers fill:#fef3c7
```

---

## Database Schema

### Posts Table (Cassandra)

```sql
CREATE TABLE posts (
    post_id UUID,
    user_id UUID,
    content TEXT,
    media_urls LIST<TEXT>,
    created_at TIMESTAMP,
    likes_count INT,
    comments_count INT,
    shares_count INT,
    PRIMARY KEY (user_id, created_at, post_id)
) WITH CLUSTERING ORDER BY (created_at DESC);

-- For fetching specific user's posts
CREATE TABLE posts_by_id (
    post_id UUID PRIMARY KEY,
    user_id UUID,
    content TEXT,
    media_urls LIST<TEXT>,
    created_at TIMESTAMP
);
```

### Feed Cache (Redis)

```
Key: feed:{user_id}
Value: Sorted Set of post_ids with scores (timestamp/rank)

ZADD feed:user123 1705312800 post:abc123
ZADD feed:user123 1705312700 post:def456

ZREVRANGE feed:user123 0 19  # Get top 20 posts
```

### Social Graph (Neo4j / Graph DB)

```
(User A)-[:FOLLOWS]->(User B)
(User A)-[:FOLLOWS]->(User C)
(User B)-[:FOLLOWS]->(User A)

Query followers:
MATCH (u:User {id: 'userB'})<-[:FOLLOWS]-(follower)
RETURN follower.id
```

---

## Feed Generation Flow

### Publishing a Post

```mermaid
flowchart TB
    U["1. User creates post"] --> PS["2. Post Service: validate, store in Posts DB, publish to Kafka"]
    PS --> FW["3. Fan-out Workers (consume Kafka)"]
    FW --> CT{"Check user type"}
    CT -->|"Regular user (<10K followers)"| Reg["For each follower: ZADD feed:#123;follower_id#125; #123;ts#125; #123;post_id#125;"]
    CT -->|"Celebrity (>10K followers)"| Cel["Only store in Posts DB (fetched on demand)"]
    Reg --> Notify["4. Notify relevant users (optional real-time)"]
    Cel --> Notify
    style PS fill:#dbeafe
    style FW fill:#fef3c7
```

### Reading Feed

```mermaid
flowchart TB
    U["1. User requests feed"] --> FS["2. Feed Service"]
    FS --> A["Get pre-computed feed from Redis: ZREVRANGE feed:#123;user_id#125; 0 19"]
    A --> B["Get list of celebrities user follows"]
    B --> C["Fetch recent posts from celebrities (pull model)"]
    C --> D["Merge all posts"]
    D --> E["Apply ranking algorithm"]
    E --> F["Return top N posts with full content"]
    F --> R["3. Return to client"]
    style FS fill:#dbeafe
    style R fill:#dcfce7
```

---

## Ranking Algorithm

```
Score = Affinity × PostWeight × TimeDecay
```

```mermaid
flowchart TB
    Score["Score = Affinity × PostWeight × TimeDecay"]
    Score --> A["Affinity (how close is this friend?): interaction frequency, profile views, mutual friends"]
    Score --> P["PostWeight (how engaging?): likes, comments (higher), shares (highest), media boost"]
    Score --> T["TimeDecay (how recent?): decay = 1 / (1 + age_hours^1.5)"]
    style Score fill:#dbeafe
```

ML-Based Ranking:
- Use historical data to train model
- Features: user interactions, post features, context
- Predict: probability of engagement

---

## Real-Time Updates

```mermaid
flowchart LR
    Client <--> WS["WebSocket Server"]
    WS <--> NS["Notification Service"]
    NS --> Redis["Redis Pub/Sub"]
    style WS fill:#dbeafe
    style Redis fill:#fef3c7
```

Flow:
1. User B posts
2. Fan-out adds to followers' feeds
3. Publish notification to Redis Pub/Sub
4. WebSocket server receives, pushes to connected clients
5. Client shows "New posts available" or auto-refreshes

---

## Caching Strategy

```mermaid
flowchart TB
    L1["L1: CDN (Static assets, public posts)"]
    L2["L2: Application Cache (Hot posts, user sessions)"]
    L3["L3: Feed Cache — Redis Cluster (pre-computed feeds, last 500 posts, TTL 24h)"]
    L4["L4: Database (Cassandra) — source of truth, historical posts"]
    L1 --> L2 --> L3 --> L4
    style L1 fill:#dcfce7
    style L2 fill:#dbeafe
    style L3 fill:#fef3c7
    style L4 fill:#fee2e2
```

---

## Handling Edge Cases

### New User

```
Problem: New user with no pre-computed feed

Solution:
1. Check for pre-computed feed → Empty
2. Generate cold-start feed:
   - Popular posts from followed users
   - Trending posts globally
   - Content based on interests
3. Trigger async feed generation
```

### Celebrity Following

```
Problem: User follows celebrity with 100M followers

Solution:
1. Don't fan-out to celebrity's followers
2. When user requests feed:
   - Pull celebrity posts on demand
   - Merge with pre-computed feed
3. Cache celebrity posts aggressively (shared by many)
```

---

## Interview Tips

- Explain push vs pull trade-offs
- Discuss hybrid approach for celebrities
- Cover ranking algorithm basics
- Mention real-time updates strategy
- Discuss caching layers
- Handle cold start for new users
