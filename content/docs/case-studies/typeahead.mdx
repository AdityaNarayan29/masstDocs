---
title: Typeahead / Autocomplete System Design
description: Design a typeahead suggestion system like Google Search.
---

## Problem Statement

Design a typeahead/autocomplete system that suggests search queries as users type.

---

## Requirements

### Functional Requirements
- Return top N suggestions for prefix
- Suggestions based on popularity/relevance
- Support personalization (optional)
- Handle multiple languages

### Non-Functional Requirements
- Ultra-low latency: under 100ms
- High availability
- Handle 100K+ QPS
- Update suggestions based on trends

---

## Capacity Estimation

```
Assumptions:
- 5 billion searches/day
- Average 4 characters typed per search
- 20 billion typeahead requests/day
- Top 10 suggestions per request

Traffic:
- 20B / 86400 ≈ 230K QPS (average)
- Peak: 500K QPS

Storage:
- 1 billion unique search terms
- Average term: 20 characters = 20 bytes
- Metadata: 30 bytes
- Total: 1B × 50 bytes = 50 GB
```

---

## High-Level Architecture

```mermaid
flowchart TB
    Client["Client (Debounce: 100-150ms)"] --> GW["API Gateway (Rate limiting, Routing)"]
    GW --> SS["Suggestion Service Cluster"]
    SS --> N1["Node 1 (In-memory Trie)"]
    SS --> N2["Node 2 (In-memory Trie)"]
    SS --> N3["Node 3 (In-memory Trie)"]
    Kafka["Kafka (Queries)"] --> Spark["Spark (Agg)"]
    Spark --> TB["Trie Builder"]
    TB -.->|Async updates| SS
    style GW fill:#dbeafe
    style SS fill:#fef3c7
    style TB fill:#dcfce7
```

---

## Trie Data Structure

### Basic Trie

```
Search terms: ["cat", "car", "card", "care", "dog"]

           (root)
           /    \
          c      d
          |      |
          a      o
         /|\     |
        t r e    g
          |
          d

Node structure:
{
  children: Map<char, Node>,
  isWord: boolean,
  suggestions: ["car", "card", "care"]  // Top N cached
}
```

### Optimized Trie with Caching

```
┌─────────────────────────────────────────────────────────┐
│                    Trie Node                             │
├─────────────────────────────────────────────────────────┤
│  char: 'c'                                              │
│  children: { 'a': Node, 'o': Node }                     │
│  isEndOfWord: false                                     │
│  topSuggestions: [                                      │
│    { term: "car", score: 10000 },                       │
│    { term: "cat", score: 8000 },                        │
│    { term: "camera", score: 5000 },                     │
│    ...                                                   │
│  ]                                                       │
└─────────────────────────────────────────────────────────┘

Pre-computed top suggestions at each node!
Query time: O(prefix length) instead of O(all descendants)
```

---

## Scoring & Ranking

`Score = w1 × frequency + w2 × recency + w3 × personalization`

```mermaid
flowchart TB
    Score["Score = w1·frequency + w2·recency + w3·personalization"]
    Score --> F["Frequency Score: global search count, normalized log(count)"]
    Score --> R["Recency Score: decay = base × e^(-λ × age_days)"]
    Score --> P["Personalization (optional): search history, location, time-of-day"]
    style Score fill:#dbeafe
```

---

## API Design

### Get Suggestions

```
GET /api/v1/suggestions?q=car&limit=10

Response:
{
  "suggestions": [
    { "text": "car insurance", "score": 0.95 },
    { "text": "car rental", "score": 0.90 },
    { "text": "car dealership", "score": 0.85 },
    { "text": "cartoon", "score": 0.80 },
    { "text": "carpet cleaning", "score": 0.75 }
  ],
  "query_time_ms": 5
}
```

---

## Client-Side Optimization

```javascript
// Debouncing - Don't send request for every keystroke
let debounceTimer;

function onInputChange(input) {
  clearTimeout(debounceTimer);
  debounceTimer = setTimeout(() => {
    fetchSuggestions(input);
  }, 150);  // Wait 150ms after last keystroke
}

// Client-side caching
const suggestionCache = new LRUCache(1000);

async function fetchSuggestions(prefix) {
  // Check cache first
  if (suggestionCache.has(prefix)) {
    return suggestionCache.get(prefix);
  }

  const suggestions = await api.getSuggestions(prefix);
  suggestionCache.set(prefix, suggestions);
  return suggestions;
}

// Prefetch next characters
function prefetchNext(prefix) {
  const likely = ['a', 'e', 'i', 'o', ' '];
  likely.forEach(char => {
    fetchSuggestions(prefix + char);
  });
}
```

---

## Data Collection Pipeline

```mermaid
flowchart TB
    S1["1. User searches 'best restaurants'"]
    S1 --> S2["2. Log to Kafka — #123;query, timestamp, user, location#125;"]
    S2 --> S3["3. Spark Streaming (5-min windows): aggregate, recency decay, filter spam"]
    S3 --> S4["4. Batch Processing (daily): merge history, rebuild trie, recalculate scores"]
    S4 --> S5["5. Trie Distribution: serialize, distribute to nodes, atomic in-memory swap"]
    style S1 fill:#dbeafe
    style S5 fill:#dcfce7
```

### Trend Detection

```mermaid
flowchart TB
    TD["Trend Detection (real-time)"]
    TD --> C["Compare: current 5-min count vs historical average"]
    C --> D{current much greater than historical?}
    D -->|Yes| Mark["Mark as trending, boost score temporarily, update trie more frequently"]
    style TD fill:#dbeafe
    style Mark fill:#dcfce7
```

---

## Trie Sharding

Strategy 1: Shard by First Character

```mermaid
flowchart LR
    SAF["Shard A-F: 'apple', 'amazon'"]
    SGM["Shard G-M: 'google', 'hello'"]
    SNZ["Shard N-Z: 'netflix', 'python'"]
    style SAF fill:#dbeafe
    style SGM fill:#fef3c7
    style SNZ fill:#dcfce7
```

Strategy 2: Shard by Hash (all replicas have full trie)

```mermaid
flowchart LR
    R1["Replica 1 (Full)"]
    R2["Replica 2 (Full)"]
    R3["Replica 3 (Full)"]
    style R1 fill:#dbeafe
    style R2 fill:#dbeafe
    style R3 fill:#dbeafe
```

- Load balance across replicas
- 50 GB fits in memory on modern servers

---

## Handling Multi-Language

```mermaid
flowchart TB
    Q["Query"] --> LD["Language Detection: user locale, character analysis (CJK, Cyrillic, etc.)"]
    LD --> EN["English Trie"]
    LD --> ES["Spanish Trie"]
    LD --> JA["Japanese Trie"]
    style LD fill:#dbeafe
```

---

## Fault Tolerance

```mermaid
flowchart TB
    FH["Failure Handling"]
    FH --> Rep["Trie Replication: 3x, across availability zones"]
    FH --> FB["Fallback Hierarchy: 1) in-memory trie, 2) Redis cache, 3) database query"]
    FH --> CB["Circuit Breaker: if latency over 100ms for 5% requests, return cached/stale results"]
    style FH fill:#dbeafe
    style CB fill:#fee2e2
```

---

## Interview Tips

- Explain trie structure and why it's optimal
- Discuss pre-computed top suggestions per node
- Cover scoring: frequency + recency + personalization
- Mention client-side optimizations (debounce, cache)
- Discuss real-time updates for trending topics
- Cover sharding strategies for scale
