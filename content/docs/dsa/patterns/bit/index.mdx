---
title: Bit Tricks
description: XOR pairing, masks as sets, n & (n-1), two's-complement tricks.
---

import { Cards, Card } from 'fumadocs-ui/components/card';

A small toolbox of bit-level tricks that show up in interview problems where the input is "numbers" but the algorithm is really about the bits.

<Cards>
  <Card title="Bit Manipulation" href="/dsa/patterns/bit/bit-manipulation" description="XOR pairing, popcount via n & (n-1), masks as sets" />
</Cards>
